// 0 = false
// 1 = true

// Teste lógico ? se verdadeiro : se falso

let num1=10
let num2=5
res=(num1 > num2 ? "Verdadeiro" : "Falso") 
console.log(res)

let st="A"
result=(st == "A" ? "Ativo" : "Inativo")
console.log(result)